services:
  postgres:
    image: postgres:16-alpine
    container_name: dotcom-omnichannel-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: dotcom_omnichannel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - dotcom_omnichannel_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dotcom_omnichannel"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  dotcom_omnichannel_pgdata:
